# vLLM Refactoring Summary - 2026-04-20

Today, I refactored the project's agent-facing documentation and integration guides to prioritize **Gemini CLI** as the recommended agentic tool for developers and contributors.

## Changes Performed:

### 1. Agent Instruction Alignment
- **Renamed** `CLAUDE.md` to `GEMINI.md` at the project root.
- **Enhanced** `GEMINI.md` with explicit, high-signal instructions for:
  - **Environment Setup:** Using `uv` for isolated Python virtual environments.
  - **Build Commands:** Fast Python-only and full C++/CUDA builds.
  - **Test Commands:** Running specific test files with `pytest`.
  - **Linting Commands:** Executing `pre-commit` hooks and `mypy` manually.

### 2. Contribution Policy Update
- **Updated `AGENTS.md`:** Changed the primary example for AI-assisted commit attribution to `Co-authored-by: gemini-cli`.
- **Updated `docs/contributing/README.md`:** Refined the AI attribution examples to consistently showcase `gemini-cli` as the leading example for contributors.

### 3. Integration Documentation Refactor
- **Replaced** `docs/serving/integrations/claude_code.md` with `docs/serving/integrations/gemini_cli.md`.
- **Documented Gemini CLI Integration:** Provided a comprehensive guide on how to:
  - Install Gemini CLI.
  - Serve high-performance models (like **Gemma 2**) via vLLM's OpenAI-compatible API.
  - Configure the `GEMINI_API_BASE` and `GEMINI_MODEL` environment variables to point at a local vLLM instance.

## Verification:
- All new/modified documentation files were verified for correct pathing and content.
- Global repository search confirmed that primary developer-facing "Claude" references have been successfully superseded or complemented by Gemini-focused documentation.

---
*Summary generated by gemini-cli.*
